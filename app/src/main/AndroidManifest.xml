<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.telkomuniversity.dph3a4.org">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.DaftarKegiatanBaru"></activity>
        <activity
            android:name=".Activities.HistoryKegiatanActivity"
            android:label="@string/title_activity_history_kegiatan"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.PresensiSuccessActivity" />
        <activity android:name=".Activities.QrScannerActivity" />
        <activity android:name=".Activities.LihatBeritaActivity" />
        <activity android:name=".Activities.TiketSuccessActivity" />
        <activity android:name=".Activities.DaftarKegiatanActivity" />
        <activity android:name=".Activities.DetailOrganisasiActivity" />
        <activity android:name=".Activities.OrganisationActivity" />
        <activity android:name=".Activities.DashboardActivity" />
        <activity android:name=".Activities.OnboardingActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>